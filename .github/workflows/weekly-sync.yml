name: Weekly Trial Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * 1"

concurrency:
  group: weekly-trial-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: production_ready_pipeline/requirements.txt

      - name: Install pipeline dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r production_ready_pipeline/requirements.txt

      - name: Validate deterministic search safety
        run: |
          node production_ready_pipeline/tests/test_schema_contract.js
          node production_ready_pipeline/tests/test_scientific_prescreen.js
          node production_ready_pipeline/tests/test_pro_review_regressions.js
          python production_ready_pipeline/tests/test_clinical_schema.py
          python production_ready_pipeline/tests/test_curator_review.py
          python production_ready_pipeline/tests/test_pro_review_regressions.py

      - name: Generate website catalog
        run: |
          mkdir -p build
          python production_ready_pipeline/gu_pipeline.py \
            --skip-excel \
            --skip-updates \
            --website-catalog-out build/website_trials.json
          python production_ready_pipeline/migrate_catalog_schema.py --catalog build/website_trials.json --write

      - name: Upload generated catalog as workflow artifact
        uses: actions/upload-artifact@v6
        with:
          name: website-trials-catalog
          path: build/website_trials.json
          if-no-files-found: error

      - name: Configure SSH for Namecheap
        env:
          NAMECHEAP_SSH_KEY: ${{ secrets.NAMECHEAP_SSH_KEY }}
          NAMECHEAP_SSH_KEY_B64: ${{ secrets.NAMECHEAP_SSH_KEY_B64 }}
        run: |
          install -m 700 -d ~/.ssh
          if [ -n "${NAMECHEAP_SSH_KEY_B64}" ]; then
            if printf '%s' "${NAMECHEAP_SSH_KEY_B64}" | tr -d '\r\n\t ' | base64 -d > ~/.ssh/id_rsa 2>/dev/null; then
              echo "Loaded SSH key from NAMECHEAP_SSH_KEY_B64"
            else
              echo "NAMECHEAP_SSH_KEY_B64 is invalid; falling back to NAMECHEAP_SSH_KEY"
              printf '%s' "${NAMECHEAP_SSH_KEY}" | tr -d '\r' > ~/.ssh/id_rsa
            fi
          else
            printf '%s' "${NAMECHEAP_SSH_KEY}" | tr -d '\r' > ~/.ssh/id_rsa
          fi
          chmod 600 ~/.ssh/id_rsa
          ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null
          cat > ~/.ssh/config <<EOF
          Host namecheap-shared
            HostName ${{ secrets.NAMECHEAP_HOST }}
            User ${{ secrets.NAMECHEAP_USER }}
            Port ${{ secrets.NAMECHEAP_PORT }}
            IdentityFile ~/.ssh/id_rsa
            IdentitiesOnly yes
            HostKeyAlgorithms +ssh-rsa
            PubkeyAcceptedAlgorithms +ssh-rsa
            StrictHostKeyChecking yes
          EOF
          chmod 600 ~/.ssh/config
          ssh-keyscan -t rsa -p "${{ secrets.NAMECHEAP_PORT }}" "${{ secrets.NAMECHEAP_HOST }}" >> ~/.ssh/known_hosts

      - name: Test SSH connectivity
        run: |
          ssh namecheap-shared "hostname -f && php -v | head -n 1"

      - name: Upload catalog to Namecheap
        run: |
          scp build/website_trials.json namecheap-shared:~/website_trials.json

      - name: Import catalog on server
        run: |
          ssh namecheap-shared \
            "php '${{ secrets.REMOTE_APP_ROOT }}/scripts/import_website_catalog.php' ~/website_trials.json && rm -f ~/website_trials.json"
